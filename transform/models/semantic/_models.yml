version: 2

models:
  - name: time_spine
    description: "Daily time spine used by the Semantic Layer (one row per day)"
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: "Date at day grain"
        granularity: day
